
/*
#include <stdio.h>
#include <string.h>

#define N 101

char grid[N][N];

int main()
{
    memset(grid, 0, sizeof(grid));
    int n, m;
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; ++i)
    {
        scanf("%s", grid[i]);
    }

    for (int j = 0; j < n; ++j)
    {
        puts(grid[j]);
    }


    return 0;
}
*/